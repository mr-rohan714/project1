<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>comments</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Comments</h1>
    <% for(let comment of comments) { %>
        <div class="contain">
            <div class="from">
                <h3>From: @<%= comment.from %></h3>
            </div>
            <div class="comment">
                <h3><%= comment.content %></h3>
            </div>
        </div>
        <% } %>
        <br><br>
        <a href="/user/addcomment/<%= id %>" class="addurl">Add Comment</a>
</body>
</html>